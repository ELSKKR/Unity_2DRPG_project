# 林溪村 — 後續開發計畫（已完成，僅供歷史參考）

這份計畫寫於書本 UI 動工之前，原本規劃四個階段：素材匯入修正、小地圖、標題動態背景、UI 全面套用。**四個階段全部已經完成**，但實際做法跟這份計畫寫的不一樣——中途決定改用另一套素材包（`Super Asset Bundle #5 Mini Pocket Status 2.1` 的書本 UI），取代了原本規劃的 133-sprite／雙相機 RenderTexture 方案。

**現況與待辦請查 `系統開發歷程.md` 系統 11 與附錄 B**，不要照這份文件的階段規劃繼續做——會跟已經完成的書本 UI 打架。

各階段對應到的實際成果：

| 這份計畫的階段 | 實際做法 | 現況 |
|---|---|---|
| 階段 0：素材匯入修正 | 沿用（`PPU=16`／`Point`／`Compression=None`，UI 素材另外用 `PPU=100`／`Single`） | ✅ 完成 |
| 階段 1：小地圖 | 改用書本 UI 的 Pixel Map 卷軸展開動畫（`MinimapController : BookMenuPanel`），不是雙相機 RenderTexture | ✅ 完成 |
| 階段 2：標題動態背景 | `TitleBackground.cs`，把 `Forest_Village` 加法式載入當標題背景 | ✅ 完成 |
| 階段 3：UI 全面套用 | 書本 UI（背包／任務／情報／設定／存檔／地圖六個分頁） | ✅ 完成（任務／情報頁版面仍待重排，見附錄 B） |
| 階段 4 小瑕疵：存檔槽場景名對應表、存檔縮圖 | 尚未做 | 已搬進附錄 B 的待辦清單 |

想看原始規劃細節（取景角度考量、9-slice 前置工時估算等）可以翻 git 歷史。
